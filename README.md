# BACKEND NODE API SERVER

## Manual Installation

Install all dependencies:

```bash
npm install
```

Set the environment variables:

```bash
cp env.default .env
```

Run the application:

```bash
npm run start
```